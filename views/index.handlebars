<div class="jumbotron border text-center p-4" style="background-color: rgba(0, 0, 0, 0.267);">
  <h1 class="display-4">Bem-vindo ao Blog do Node</h1>
  <p class="lead">Este é um BLOG simples, desenvolvido com Node.js + Express + MongoDB.</p>
  <hr class="my-4">
  <p>Aqui você vai poder ler sobre as novidades do mundo da computação.</p>
  <a class="btn btn-primary btn-lg m-2" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>

<h2 class="mt-3">Postragens recentes:</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
        <small>Data de publicação: {{data}}</small>
    </div>
</div>
{{else}}
  <h5>Nenhuma postagem</h5>
{{/each}}